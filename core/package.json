{
  "name": "@codecharter/core",
  "version": "0.0.0",
  "private": true,
  "description": "Core engine for codecharter — map generation, geohash addressing, resolver, tiles, selections, activity, and the hardened localhost HTTP server + CLI.",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts",
    "./geohash": "./src/main/geohash.ts"
  },
  "scripts": {
    "test": "tsx --test src/test/*.test.ts",
    "typecheck": "tsgo --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
