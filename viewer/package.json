{
  "name": "@codecharter/viewer",
  "version": "0.0.0",
  "private": true,
  "description": "Browser viewer for codecharter — canvas SPA (app shell) over the decomposed render model + deep-link codec; bundled with esbuild.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/test/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.24.2",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
